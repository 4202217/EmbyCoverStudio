# 更新记录

## v1.2.1（2026-08-11）

- 更新记录弹窗支持 Markdown 渲染（标题、列表、加粗、行内代码）

## v1.2.0（2026-08-11）

### 概览
- 重构为「健康面板」：只展示需要关注的内容（封面生成异常 + 最近失败任务），支持单项已读、一键清除、失败重试
- 失败主动提醒：同步/更新结束后有失败自动弹提示，侧边栏状态点变红并显示异常数量
- 任务记录从概览移至「运行记录」页，与系统日志合并

### 运行记录
- 任务记录与运行日志使用统一的表格组件：表头点击排序、按列筛选、固定高度内部滚动
- 任务记录默认按最新在前排序

### 封面管理
- 移除行首复选框，改为点击行空白处单选/取消选择
- 单选后修改任意配置（样式/选图依据/手动选片）立即生成本地预览图，保存后才推送 Emby
- 手动选片自动弹出选择窗口，本地预览，保存（或保存并锁定）后上传
- 选图依据新增「随机」：纯随机，每次更新重新随机挑选海报
- 锁定/取消锁定操作保持在单选状态；更新按钮加载时尺寸不变

### 封面配置
- 三套配置（媒体库·单图海报 / 媒体库·海报墙 / 合集·单图海报）UI 重排：小标题 + 选项 + 说明三行式
- 背景模式改为按钮，渐变背景选色并入背景模式节内
- 未单独配置的媒体库默认样式改为按钮选择
- 移除字体配置项
- 实时预览固定在右侧，保存按钮移至预览下方

### 其他
- 界面去除 emoji，统一使用矢量 SVG 图标
- 海报墙样式间距微调

## v1.1.0（2026-08-11）

### 封面生成
- 新增「海报墙」样式（媒体库可选），倾斜海报墙可配置文字、尺寸与溢出效果
- 封面配置按「媒体库·单图海报 / 媒体库·海报墙 / 合集·单图海报」三套独立设置（选图依据、字号、颜色、字体、背景模式等）
- 支持手动选择封面影片，保存配置后自动锁定（锁定后不参与自动监控）
- 同步时自动清理 Emby 中已不存在的媒体库/合集；封面外部变化自动检测并重新生成

### 管理界面
- 「封面管理」支持搜索与多维度下拉筛选（类型/状态/配置/封面），并带矢量图标
- 任务记录、运行日志改为固定高度内部滚动
- 侧边栏显示当前版本号
- 设置页新增「配置与数据备份」：一键导出/导入全部配置与数据
- Webhook 测试通知改为真实接收检测（在 Emby 插件里点击测试，本页面显示是否收到）

## v1.0.0

- Emby 媒体库/合集封面自动生成工具首版
- 单图海报样式、Webhook 入库自动更新、cron 定时同步、任务记录、Docker 一键部署
