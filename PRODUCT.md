# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

自托管 Emby 媒体服务器（NAS / 家庭影音库）的所有者与日常维护者。他们在内网或公网部署 Emby，希望媒体库与合集的封面随影片入库、更新自动保持美观与统一，而不是手动逐张挑选海报。

主要使用情境：部署完成后做一次性配置（Emby 连接、全局封面样式、Webhook），之后日常只查看概览、处理异常、偶尔批量调整或锁定个别目标。

## Product Purpose

自动为 Emby 媒体库与合集生成并写回封面：从库内影片海报中挑选一张（或一组），合成「海报 + 库名标题 + 数量副标题」的封面，并通过 Emby API 上传替换。成功的标志：封面与影片动态保持一致，日常无需人工干预。

## Positioning

围绕「精准触发 + 差异更新 + 可控覆盖」的自动化封面系统：

- Webhook 在影片入库/更新时精准定位受影响的媒体库与合集，只重新生成相关封面；定位失败自动全量兜底。
- 定时同步使用指纹对比，仅在内容或模板配置变化时重新生成，避免无意义重复上传。
- 提供风格预设（单图海报 / 大标题 / 海报墙）与逐目标锁定、手动选片覆盖，自动化与人工控制并存。

邻近工具无法在不实现相同触发、指纹对比与写回链路的情况下复制这一机制。

## Operating Context

- 部署形态：Docker（amd64 / arm64）或 Node standalone，默认端口 3000（compose 示例常用 9308），数据持久化在 `data/`（`db.json`、`covers/`、`cache/`）。
- 前置依赖：Emby 管理员 API 密钥；Webhook 需要 Emby 官方 Webhooks 插件；容器内置 Noto Sans CJK 字体，本地运行依赖系统中文字体。
- 日常运维：侧边栏版本号检查 GitHub 新版本并查看 changelog；备份通过导出/导入 JSON 或 WebDAV 自动/手动同步（不含封面图片）。
- 安全模型：默认不启用访问令牌，任何能访问服务端口的人都能操作；可选启用令牌保护管理界面，建议内网或反代后部署。

## Capabilities and Constraints

- 能力：自动发现媒体库与合集（含总合集 BoxSets）；搜索与类型/状态/配置/封面四维筛选；多选批量操作（锁定、取消锁定、恢复默认配置、更新封面）；逐目标配置样式与选图依据（最新入库 / 最新发行 / 随机 / 手动选择，手动保存后自动锁定）；三套全局封面配置（媒体库·单图海报 / 媒体库·海报墙 / 合集·单图海报）带实时预览；Webhook 精准更新（默认 20 秒防抖）；cron 定时同步（默认每 6 小时）；任务记录与运行日志（排序/筛选）；新版本检测；可选访问令牌。
- 约束：封面输出固定比例（媒体库 16:9、合集 2:3），格式 PNG / WebP；写回依赖 Emby API 能力，部分版本上传失败时封面保留在 `data/covers/` 供手动设置；备份不包含封面图片；Jellyfin 理论上兼容但以 Emby 为准。
- 术语：单图海报 / 大标题 / 海报墙（wall-v）、选图依据、锁定、指纹对比、Webhook 防抖。

## Brand Commitments

- 名称：Emby 封面工坊（Emby Cover Studio），副标题「封面生成器」。
- 界面语言：简体中文。
- 已确立并多次迭代的视觉身份：暗色影院 / 指挥中心风格（「Cinema Wall」品牌语、青色主色 + 金色点缀、封面墙与动态取色光晕）。视觉细节固化在 `DESIGN.md`。

## Evidence on Hand

- `README.md`：功能特性、快速开始、首次使用、Webhook 配置、常见问题与安全说明（已核对）。
- `docker-compose.yml` / `Dockerfile`：镜像构建与运行方式（standalone、`/app/data` 数据目录、健康检查）。
- `CHANGELOG.md`：版本演进记录（v1.5.x – v1.6.0，UI 迭代历史）。
- `scripts/mock-emby.js` + `test-integration.js`：模拟 Emby 的端到端测试。
- `data/`：真实运行数据（封面图、任务记录、配置）。
- 推断标记：用户画像、定位表述、产品原则由仓库证据（README、功能实现、UI 文案、目录结构）推断得出，尚未经用户逐条确认；若与实际情况不符，请指正后更新本文件。

## Product Principles

1. 自动化优先：能由事件/定时触发并差异更新的，就不依赖人工逐张操作。
2. 人工控制永远存在：锁定、手动选片、恢复默认与显式确认构成自动化之外的兜底与回退。
3. 状态可见：概览、任务记录、日志、进度与异常提示让系统行为可审计。
4. 数据安全：备份/恢复与覆盖类操作必须有明确确认，数据目录结构可移植。
5. 低摩擦部署：Docker 一键、内置字体、单容器自包含（仅版本检测访问 CDN）。

## Accessibility & Inclusion

- 界面为简体中文；未建立正式无障碍标准。
- 已实现的基础能力：键盘焦点可见、弹窗焦点陷阱与 Esc 关闭、aria 语义（表格排序、进度、状态）、`prefers-reduced-motion` 尊重。
